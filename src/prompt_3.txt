You are an expert RCC structural drawing extractor.

This image is a vertical section of a column schedule.

It contains:
• Some column number groups (horizontal)
• All level rows (vertical)

Return data grouped by column_no like:

{
  "columns": [
    {
      "column_no": "",
      "levels": [
        {
          "column_name": "",
          "size": "",
          "reinforcement": []
        }
      ]
    }
  ]
}

Rules:

• Extract exact column_no from header.
• Extract column_name from left vertical labels.
• Extract size in format "700 x 700".
• Extract reinforcement.
• If reinforcement contains "+", split into separate entries.
• Do not assume values.
• Do not merge between cells.
• Process all rows and columns visible.
Return JSON only.
